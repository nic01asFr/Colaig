# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
itsdangerous>=2.1.0
python-multipart>=0.0.6

# Matrix/Tchap (avec support E2E — nécessite libolm-dev)
matrix-nio[e2e]>=0.24.0

# Vector search
faiss-cpu>=1.7.4
numpy>=1.24.0
rank-bm25>=0.2.2

# HTTP client (WebDAV + Albert API)
httpx>=0.25.0

# Document extraction
pymupdf>=1.23.0
python-docx>=1.0.0
beautifulsoup4>=4.12.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging
structlog>=23.0.0

# MCP (Model Context Protocol) server
mcp[cli]>=1.8.0

# Box storage (optionnel — requis si STORAGE_BACKEND=box)
box-sdk-gen>=1.4.0

# S3/MinIO storage (optionnel — requis si STORAGE_BACKEND=s3)
#
# Son absence ne se voyait qu'a chaud : la configuration valide, `create_storage`
# construit l'objet, et l'ImportError ne survient qu'au premier acces au stockage —
# donc dans la sonde de disponibilite, en pod qui ne devient jamais pret. Releve le
# 29/08/2026 en branchant le seau MinIO de SSPCloud.
boto3>=1.26.0

# Dev dependencies
# pip install -e ".[dev]"
